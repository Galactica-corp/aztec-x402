{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; OX=\"$CLAUDE_PROJECT_DIR/node_modules/.bin/oxlint\"; [ -x \"$OX\" ] || exit 0; case \"$f\" in *.ts|*.tsx|*.js|*.jsx) \"$OX\" --config \"$CLAUDE_PROJECT_DIR/.oxlintrc.json\" \"$f\" ;; esac; }",
            "statusMessage": "Linting edited file"
          }
        ]
      }
    ]
  }
}
